{
  "_comment": "Cursor hooks. Copy to .cursor/hooks.json. CLAUDE_HARNESS=cursor switches the session-start JSON shape. See templates/hooks/README.md for details.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "command": "CLAUDE_HARNESS=cursor bash templates/hooks/session-start",
        "async": false
      }
    ],
    "PreToolUse": [
      {
        "_comment": "Optional gate on rules/agent-behavior.md edits. Cursor's PreToolUse contract may differ from Claude Code's — verify hook fires before relying on it.",
        "matcher": "Write|Edit",
        "command": "bash templates/hooks/agent-behavior-gate.sh",
        "async": false
      }
    ]
  }
}
