{
  "_comment": "Claude Code hooks. Copy to .claude/hooks.json. See templates/hooks/README.md for details.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "command": "bash templates/hooks/session-start",
        "async": false
      }
    ],
    "PreToolUse": [
      {
        "_comment": "Optional gate on rules/agent-behavior.md edits. Remove this block to disable.",
        "matcher": "Write|Edit",
        "command": "bash templates/hooks/agent-behavior-gate.sh",
        "async": false
      }
    ]
  }
}
