{
  "_comment": "Claude Code project-wide settings. Hook schema uses nested hooks[].{type,command}. SessionStart re-injects SKILL.md.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-start"
          }
        ]
      }
    ]
  }
}
